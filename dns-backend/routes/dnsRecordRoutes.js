// dnsRecordRoutes.js
const express = require('express');
const router = express.Router();
const DNSRecord = require('../models/DNSRecord');

// CRUD operations for DNS records
// Example routes: GET /dns-records, POST /dns-records, PUT /dns-records/:id, DELETE /dns-records/:id

module.exports = router;
